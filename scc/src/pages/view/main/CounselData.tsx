import { Card } from 'antd';
import { useEffect } from 'react';

const CounselData = () => {

    useEffect(() => {

    }, );

    return (
        <Card title="상담 정보" style={{ height: '100%', overflow: 'auto' }}></Card>
    );
};

export default CounselData;